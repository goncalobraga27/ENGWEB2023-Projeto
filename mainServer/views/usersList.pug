extends layout

block content
  .w3-card-4
    header.w3-container.w3-teal
      h1 Users List [Nº de Users: #{u.length}]
    .w3-container
      table.w3-table-all
        tr
          th DateCreated
          th Name
          th Username
          th Level 
        
        each user in u
          tr
            td= user.dateCreated
            td= user.name
            td= user.username
            td= user.level
    footer.w3-container.w3-teal
      h5 Generated for TP_EngWeb2023 in #{d}
    